# OpenAI API Key (Optional: pipeline evaluates offline 100% deterministically using results/cache/)
OPENAI_API_KEY=

# LLM Model Configuration (Default: gpt-4o-mini)
LLM_MODEL=gpt-4o-mini

# Data Configuration (Set to 1 only if full 106k threads parquet is available locally)
USE_FULL_DATASET=0
